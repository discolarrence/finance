pk_0e0ab8173c88410ea4a4a5c1b64dddbc

CREATE TABLE 'history' (
'id' integer PRIMARY KEY AUTOINCREMENT NOT NULL, 
'user_id' integer NOT NULL, 
'datetime' datetime NOT NULL DEFAULT CURRENT_TIMESTAMP, 
'action' varchar(4) NOT NULL, 
'symbol' varchar(5) NOT NULL, 
'price' integer NOT NULL)

stock name, total shares, current price, total value
cash balance
total equity
("SELECT stocks.id, name, symbol, total_shares FROM stocks JOIN current ON stocks.id = current.stock_id WHERE user_id = ?", session["user_id"])